import sys
print "write a line:"
text = print input([-])


from sys 
for c in text:
	if c == "(":
		temp = []
		for c in text:
			if c == ")":
				for c in text:
					if c == int:
						temp.append(c)
	text = temp
print text